//1. Constructor name is as same as the class name
//2. Cannot have return value/void.
//3. constructor will be called when you create the object
//4. What is constructor? To restrict the unnecessary object creations. 

// Note: We don't have constructor but we are calling the object what will be
// the o/p?
// It will be blank. Internally it will be calling an hidden/default constructor

// Note: If you have your own/custom constructors then default constructor will
// be not shown.

// We can overload the constructor

// https://www.javatpoint.com/difference-between-final-finally-and-finalize